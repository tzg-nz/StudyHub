# ============================================
# Django 基础配置
# ============================================
django_secret_key=your-secret-key-here-please-change-in-production
debug=True
allowed_hosts=localhost,127.0.0.1

# ============================================
# 数据库配置 (MySQL)
# ============================================
# 开发环境默认使用 SQLite，生产环境建议使用 MySQL
# db_engine=django.db.backends.mysql
# db_name=studyhub
# db_user=root
# db_password=your-password
# db_host=localhost
# db_port=3306

# ============================================
# CORS 配置 (生产环境必须设置)
# ============================================
cors_allow_all_origins=True
# 生产环境示例：
# cors_allow_all_origins=False
# cors_allowed_origins=https://yourdomain.com,https://www.yourdomain.com

# ============================================
# CSRF 配置 (生产环境必须设置)
# ============================================
csrf_trusted_origins=http://localhost:5173,http://localhost:5174,http://127.0.0.1:5173,http://127.0.0.1:5174
# 生产环境示例：
# csrf_trusted_origins=https://yourdomain.com,https://www.yourdomain.com

# ============================================
# Vercel 部署配置（在 Vercel Dashboard 设置）
# ============================================
# 必须设置的变量（全部小写）：
# django_secret_key=生成一个50位随机字符串
# debug=False
# allowed_hosts=authenticationcenter.vercel.app,*.vercel.app
# database_url=postgresql://user:password@host:port/database
# cors_allow_all_origins=False
# cors_allowed_origins=https://authenticationcenter.vercel.app
# csrf_trusted_origins=https://authenticationcenter.vercel.app

# ============================================
# 传统服务器部署配置示例
# ============================================
# django_secret_key=生成一个50位随机字符串
# debug=False
# allowed_hosts=yourdomain.com,www.yourdomain.com
# cors_allow_all_origins=False
# cors_allowed_origins=https://yourdomain.com,https://www.yourdomain.com
# csrf_trusted_origins=https://yourdomain.com,https://www.yourdomain.com
# db_engine=django.db.backends.mysql
# db_name=studyhub
# db_user=studyhub_user
# db_password=your-secure-password
# db_host=localhost
# db_port=3306
